import styled from 'styled-components';
import { SwipeableDrawer } from '@mui/material';

export const Drawer = styled(SwipeableDrawer)`
padding:20px;
`;

export const ContainerItemsDrawer = styled.div`
padding:20px;
`;

export const Title = styled.p``;